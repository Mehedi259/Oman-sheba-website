# apps/api

Reserved location for the Django REST Framework backend.

This directory is intentionally empty except for this file. The full build
spec lives at [`docs/DJANGO_BACKEND_MASTER_PROMPT.md`](../../docs/DJANGO_BACKEND_MASTER_PROMPT.md) —
hand that file to an AI coding agent (or follow it yourself) to scaffold the
Django project here.
